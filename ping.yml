---
- hosts: all
  tasks:
          - name: ping1
            ping:
                    data: "{{ item }}"
            with_items:
                    - one
                    - two
                    - three

          - name: ping2
            ping:
